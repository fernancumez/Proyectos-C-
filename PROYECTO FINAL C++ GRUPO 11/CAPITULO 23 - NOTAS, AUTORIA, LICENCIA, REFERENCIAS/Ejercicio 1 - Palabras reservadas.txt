/*
	Ejercicio 1 -  Palabras reservadas
*/

#include<iostream>

using namespace std;

int main(){
	/*	
		Todas las palabras reservadas, simpre deben estar
		en minuscula
	*/
	
	// A continuación una lista de todas las palabras reservadas
	/*
		asm auto bool break case catch char class const const_cast continue default delete
		do double dynamic_casts else enum extern explicit false float for friend goto if
		inline int long mutable namespace new operator private protected public register
		reinterpret_cast return short signed sizeof static static_class struct switch template
		this throw true try typedef typeid union unsigned using virtual void volatile
		wchar_t while
	*/
	
	return 0;
}